source 'https://supermarket.getchef.com'

metadata

cookbook 'apt', path: "#{ENV['CHEF_REPO']}/cookbooks/apt/"
cookbook 's3', path: "#{ENV['CHEF_REPO']}/cookbooks/s3/"
cookbook 'rbenv', git: 'git://github.com/RiotGamesCookbooks/rbenv-cookbook'
